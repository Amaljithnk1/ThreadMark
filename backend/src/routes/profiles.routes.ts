import {Router} from "express";import {getBuyer,getSupplier,saveBuyer,saveSupplier,uploadVerification} from "../controllers/profiles.controller.js";import {requireRole} from "../middleware/auth.middleware.js";const router=Router();router.get("/buyer",...requireRole("buyer"),getBuyer);router.put("/buyer",...requireRole("buyer"),saveBuyer);router.get("/supplier",...requireRole("supplier"),getSupplier);router.put("/supplier",...requireRole("supplier"),saveSupplier);router.post("/supplier/verification-documents",...requireRole("supplier"),uploadVerification);export default router;
