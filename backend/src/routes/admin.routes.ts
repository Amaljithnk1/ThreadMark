import {Router} from "express";import {analytics,buyers,moderateProduct,orders,overview,products,setSupplierStatus,setVerification,suppliers,flagDocuments} from "../controllers/admin.controller.js";import {requireRole} from "../middleware/auth.middleware.js";const router=Router();router.use(...requireRole("admin"));router.get("/overview",overview);router.get("/suppliers",suppliers);router.patch("/suppliers/:id/status",setSupplierStatus);router.patch("/suppliers/:id/verification",setVerification);router.patch("/suppliers/:id/documents-flag",flagDocuments);router.get("/buyers",buyers);router.get("/orders",orders);router.get("/products",products);router.patch("/products/:id/moderation",moderateProduct);router.get("/analytics",analytics);export default router;
