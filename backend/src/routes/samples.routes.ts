import {Router} from "express";import {buyerSamples,createSample,supplierSamples,updateSampleStatus} from "../controllers/samples.controller.js";import {requireRole} from "../middleware/auth.middleware.js";const router=Router();router.post("/",...requireRole("buyer"),createSample);router.get("/",...requireRole("buyer"),buyerSamples);router.get("/supplier",...requireRole("supplier"),supplierSamples);router.patch("/:id/status",...requireRole("supplier"),updateSampleStatus);export default router;
