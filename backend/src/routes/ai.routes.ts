import {Router} from "express";import {optionalAuth,requireRole} from "../middleware/auth.middleware.js"; import {chat,compareProducts,naturalSearch,recommendations,semanticSearch,useCaseMatch,warm} from "../controllers/ai.controller.js"; const router=Router(); router.post("/warm",warm);router.post("/chat",optionalAuth,chat);router.post("/natural-search",naturalSearch);router.post("/semantic-search",semanticSearch);router.get("/recommendations",...requireRole("buyer"),recommendations);router.post("/compare",...requireRole("buyer"),compareProducts);router.post("/use-case-match",useCaseMatch);export default router;
