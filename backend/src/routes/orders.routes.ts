import {Router} from "express";import {buyerOrders,supplierOrders,updateStatus} from "../controllers/orders.controller.js";import {requireRole} from "../middleware/auth.middleware.js";const router=Router();router.get("/buyer",...requireRole("buyer"),buyerOrders);router.get("/supplier",...requireRole("supplier"),supplierOrders);router.patch("/:id/status",...requireRole("supplier"),updateStatus);export default router;
