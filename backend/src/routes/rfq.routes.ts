import {Router} from "express";import {createRfq,decideQuote,listBuyerRfqs,listSupplierRfqs,quoteRfq,rejectRfq} from "../controllers/rfq.controller.js";import {requireRole} from "../middleware/auth.middleware.js";const router=Router();router.post("/",...requireRole("buyer"),createRfq);router.get("/buyer",...requireRole("buyer"),listBuyerRfqs);router.get("/supplier",...requireRole("supplier"),listSupplierRfqs);router.post("/:rfqId/quotes",...requireRole("supplier"),quoteRfq);router.post("/:rfqId/reject",...requireRole("supplier"),rejectRfq);router.post("/quotes/:quoteId/decision",...requireRole("buyer"),decideQuote);export default router;
