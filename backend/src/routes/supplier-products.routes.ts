import {Router} from "express";import {create,listMine,remove,suggestCategory,update} from "../controllers/supplier-products.controller.js";import {requireRole} from "../middleware/auth.middleware.js";const router=Router();router.use(...requireRole("supplier"));router.get("/",listMine);router.post("/",create);router.post("/suggest-category",suggestCategory);router.put("/:id",update);router.delete("/:id",remove);export default router;
