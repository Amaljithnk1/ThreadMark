"use client";
import {useEffect,useState} from "react";import {SiteHeader} from "@/components/site-header";import {rfqQuoteSchema} from "@threadmark/shared";import {api} from "@/lib/api";import {RequireRole} from "@/components/require-role";
type Rfq={id:string;product_name:string|null;custom_spec:Record<string,unknown>|null;quantity:number;target_price:number|string|null;needed_by_date:string|null;status:string;created_at:string};
export default function SupplierRfqs(){const [rfqs,setRfqs]=useState<Rfq[]>([]);const [notice,setNotice]=useState('');const [quoteFor,setQuoteFor]=useState<string|null>(null);const [price,setPrice]=useState('');const [leadTime,setLeadTime]=useState('');const [notes,setNotes]=useState('');async function load(){try{const r=await api<{data:Rfq[]}>('/rfqs/supplier');setRfqs(r.data)}catch(cause){setNotice(cause instanceof Error?cause.message:'RFQs could not be loaded.')}}useEffect(()=>{const id=window.setTimeout(()=>void load(),0);return()=>window.clearTimeout(id)},[]);async function sendQuote(e:React.FormEvent){e.preventDefault();if(!quoteFor)return;try{const payload=rfqQuoteSchema.safeParse({quotedPrice:Number(price),quotedLeadTimeDays:Number(leadTime),notes});if(!payload.success)throw new Error(payload.error.issues[0]?.message??'Check your quote.');await api(`/rfqs/${quoteFor}/quotes`,{method:'POST',body:JSON.stringify(payload.data)});setRfqStatus(quoteFor,'quoted');setQuoteFor(null);setNotice('Structured quote sent. The buyer can accept or reject it in the marketplace.')}catch(cause){setNotice(cause instanceof Error?cause.message:'Quote could not be sent.')}}async function reject(id:string){try{await api(`/rfqs/${id}/reject`,{method:'POST'});setRfqStatus(id,'rejected');setNotice('RFQ rejected. No personal contact information was shared.')}catch(cause){setNotice(cause instanceof Error?cause.message:'RFQ could not be rejected.')}}function setRfqStatus(id:string,status:string){setRfqs(list=>list.map(item=>item.id===id?{...item,status}:item))}return <RequireRole role="supplier"><><SiteHeader/><main className="mx-auto max-w-6xl px-5 py-10 md:px-8"><p className="font-data text-xs uppercase tracking-[.14em] text-ochre">Supplier negotiation desk</p><h1 className="mt-2 font-display text-5xl">RFQ inbox</h1><p className="mt-3 max-w-2xl leading-7">Respond with pricing, lead time, and notes through the marketplace. Buyer and supplier personal contact details remain private.</p>{notice&&<p role="status" className="mt-5 border border-ochre/40 bg-ochre/10 p-3 text-sm">{notice}</p>}<div className="mt-8 space-y-5">{rfqs.length?rfqs.map(rfq=><article key={rfq.id} className="border border-loom bg-[#f7f1e7] p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-data text-xs uppercase tracking-[.13em] text-ochre">{rfq.product_name?'Catalogue RFQ':'Custom fabric specification'}</p><h2 className="mt-2 font-display text-2xl">{rfq.product_name??String(rfq.custom_spec?.description??'Custom requirement')}</h2></div><span className="font-data text-xs uppercase">{rfq.status}</span></div><dl className="mt-5 grid gap-3 text-sm sm:grid-cols-3"><div><dt className="text-walnut/70">Quantity</dt><dd className="mt-1 font-data">{rfq.quantity} m</dd></div><div><dt className="text-walnut/70">Target price</dt><dd className="mt-1 font-data">{rfq.target_price?`₹${rfq.target_price}/m`:'Not specified'}</dd></div><div><dt className="text-walnut/70">Needed by</dt><dd className="mt-1 font-data">{rfq.needed_by_date??'Flexible'}</dd></div></dl>{rfq.status==='open'&&<div className="mt-5 flex gap-3"><button onClick={()=>setQuoteFor(rfq.id)} className="rounded-sm bg-indigo-dye px-4 py-2.5 font-semibold text-cotton">Send quote</button><button onClick={()=>void reject(rfq.id)} className="rounded-sm border border-danger px-4 py-2.5 font-semibold text-danger">Reject</button></div>}{quoteFor===rfq.id&&<form onSubmit={sendQuote} className="mt-5 grid gap-4 border-t border-loom pt-5 sm:grid-cols-3"><label><span className="mb-1 block text-sm font-semibold">Quoted ₹/m</span><input value={price} onChange={e=>setPrice(e.target.value)} type="number" min="0" required className="w-full border border-loom bg-cotton p-2.5"/></label><label><span className="mb-1 block text-sm font-semibold">Lead time (days)</span><input value={leadTime} onChange={e=>setLeadTime(e.target.value)} type="number" min="0" required className="w-full border border-loom bg-cotton p-2.5"/></label><label><span className="mb-1 block text-sm font-semibold">Notes</span><input value={notes} onChange={e=>setNotes(e.target.value)} className="w-full border border-loom bg-cotton p-2.5"/></label><div className="sm:col-span-3"><button className="rounded-sm bg-indigo-dye px-4 py-2.5 font-semibold text-cotton">Send structured quote</button></div></form>}</article>):<div className="border border-loom bg-[#f7f1e7] p-8">No open RFQs are available for your catalogue.</div>}</div></main></RequireRole>}}
