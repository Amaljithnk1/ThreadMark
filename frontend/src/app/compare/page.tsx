"use client";import {useEffect,useState} from "react";import Link from "next/link";import {SiteHeader} from "@/components/site-header";import {api} from "@/lib/api";import {RequireRole} from "@/components/require-role";
type Product={id:string;name:string;category:string;gsm:number|string;composition:string;weave_type:string;width:string;shrinkage_rate:string;colorfastness_rating:string;stock_qty:number;stock_type:string;lead_time_days:number;certifications:string[];price:number|string;sustainability_tags:string[];business_name:string;verification_status:string};const fields:[keyof Product,string][]=[['gsm','GSM'],['composition','Composition'],['weave_type','Weave'],['width','Width'],['shrinkage_rate','Shrinkage'],['colorfastness_rating','Colourfastness'],['stock_qty','Available stock'],['stock_type','Stock type'],['lead_time_days','Lead time'],['certifications','Certifications'],['price','Price / metre'],['sustainability_tags','Sustainability']];
export default function ComparePage(){const [products,setProducts]=useState<Product[]>([]);const [notice,setNotice]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{const timer=window.setTimeout(()=>{const stored=sessionStorage.getItem("threadmark-compare-products");if(stored){try{const productIds=JSON.parse(stored) as string[];sessionStorage.removeItem("threadmark-compare-products");if(productIds.length>=2){void api<{data:{products:Product[]}}>("/ai/compare",{method:"POST",body:JSON.stringify({productIds})}).then(r=>{setProducts(r.data.products);setNotice("Comparison created and recorded for marketplace analytics.")}).catch(c=>setNotice(c instanceof Error?c.message:"Products could not be compared.")).finally(()=>setLoading(false))}else{setLoading(false)}}catch{sessionStorage.removeItem("threadmark-compare-products");setLoading(false)}}else{setLoading(false)}},0);return()=>window.clearTimeout(timer)},[]);return <RequireRole role="buyer"><><SiteHeader/><main className="mx-auto max-w-7xl px-5 py-10 md:px-8"><p className="font-data text-xs uppercase tracking-[.14em] text-ochre">Buyer decision tool</p><h1 className="mt-2 font-display text-5xl">Compare materials</h1><p className="mt-3 max-w-2xl leading-7">Review documented material facts side by side. Comparison events improve marketplace analytics without exposing supplier private data.</p>{loading&&<p className="mt-7 text-sm">Loading comparison…</p>}{!loading&&products.length===0&&<div className="mt-7 max-w-2xl border border-loom bg-[#f7f1e7] p-6"><p className="text-sm leading-6">Select 2–4 materials on the marketplace using the "Compare this material" checkboxes, then choose "Compare selected" to see them here.</p><Link href="/marketplace" className="mt-4 inline-block rounded-sm bg-indigo-dye px-4 py-2.5 font-semibold text-cotton">Browse the marketplace</Link></div>}{notice&&<p role="status" className="mt-4 border border-ochre/40 bg-ochre/10 p-3 text-sm">{notice}</p>}{products.length>0&&<div className="mt-8 overflow-x-auto border border-loom bg-[#f7f1e7]"><table className="w-full min-w-[760px] border-collapse text-left text-sm"><thead><tr className="border-b border-loom"><th className="p-4 font-data text-[11px] uppercase">Specification</th>{products.map(p=><th key={p.id} className="p-4"><span className="font-display text-xl">{p.name}</span><span className="mt-1 block text-xs font-normal">{p.business_name}{p.verification_status==='verified'?' · Verified':''}</span></th>)}</tr></thead><tbody>{fields.map(([key,label])=><tr key={key} className="border-b border-loom/60"><th className="p-4 font-data text-xs uppercase tracking-wide text-walnut/70">{label}</th>{products.map(p=><td key={p.id} className="p-4 font-data">{Array.isArray(p[key])?(p[key] as string[]).join(', ')||'—':String(p[key]??'—')}</td>)}</tr>)}</tbody></table></div>}</main></></RequireRole>}
