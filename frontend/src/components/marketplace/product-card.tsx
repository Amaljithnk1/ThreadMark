import Image from "next/image";
import Link from "next/link";
export interface ProductPreview { id: string; name: string; category: string; composition: string; gsm: number; price: number; stockType: "ready_stock" | "made_to_order"; certifications: string[]; image: string; supplier: string; verified?: boolean; sustainabilityTags?: string[]; }
export function ProductCard({ product }: { product: ProductPreview }) { return <article className="swatch-tag swatch-notch overflow-hidden border border-loom bg-[#f7f1e7] transition-transform hover:-translate-y-1"><div className="relative aspect-[4/3] overflow-hidden bg-loom/30"><Image src={product.image} alt={`Fabric sample: ${product.name}`} fill sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw" className="object-cover transition-transform duration-300 hover:scale-105" /></div><div className="p-4"><div className="flex items-start justify-between gap-2"><p className="font-data text-[11px] uppercase tracking-[.1em] text-ochre">{product.category}</p><span className={`font-data text-[10px] uppercase ${product.stockType === "ready_stock" ? "text-success" : "text-warning"}`}>{product.stockType === "ready_stock" ? "Ready stock" : "Made to order"}</span></div><h3 className="mt-2 font-display text-xl leading-tight"><Link href={`/products/${product.id}`} className="after:absolute after:inset-0">{product.name}</Link></h3><p className="mt-2 text-sm">{product.composition} · {product.gsm} GSM</p><div className="mt-4 flex items-end justify-between gap-2 border-t border-loom/70 pt-3"><div><p className="font-data text-xs text-walnut/70">from</p><p className="font-data text-lg font-semibold">₹{product.price}/m</p></div><p className="max-w-28 text-right text-xs leading-4">{product.verified ? "Verified " : ""}{product.supplier}</p></div></div></article>; }
