"use client";
import {useEffect,useRef,useState} from "react";
import {api} from "@/lib/api";
import {useAIStore} from "@/stores/ai-store";
declare global { interface Window { webkitSpeechRecognition?: new()=>SpeechRecognition; SpeechRecognition?: new()=>SpeechRecognition; } }
interface SpeechRecognition extends EventTarget { continuous:boolean; interimResults:boolean; lang:string; start():void; onresult:(event:{results:{[index:number]:{[index:number]:{transcript:string}}}})=>void; onerror:()=>void; }
export function AssistantWidget(){const {messages,isThinking,isOpen,setOpen,add,setThinking}=useAIStore();const [text,setText]=useState("");const [voiceError,setVoiceError]=useState("");const feed=useRef<HTMLDivElement>(null);useEffect(()=>{void api("/ai/warm",{method:"POST"}).catch(()=>undefined);},[]);useEffect(()=>feed.current?.scrollTo({top:feed.current.scrollHeight}),[messages,isThinking]);async function send(value=text){const trimmed=value.trim();if(!trimmed||isThinking)return;setText("");add({role:"user",content:trimmed});setThinking(true);try{if(/^(?:semantic|similar to|inspiration)\b/i.test(trimmed)){const semantic=await api<{data:{products:{name:string;composition:string;gsm:number|string;price:number|string}[]}}>("/ai/semantic-search",{method:"POST",body:JSON.stringify({query:trimmed.replace(/^(?:semantic|similar to|inspiration)\s*:?[\s]*/i,"")})});const results=semantic.data.products.slice(0,4).map(item=>`${item.name} — ${item.composition}, ${item.gsm} GSM, ₹${item.price}/m`).join("\n");add({role:"assistant",content:results?`Semantic matches:\n${results}`:"I could not find semantic matches."});return;}if(/(?:fabric|material).*(?:for|used for)|(?:for).*(?:cushion|uniform|outdoor|curtain|upholstery|apparel)/i.test(trimmed)){const match=await api<{data:{matches:{name:string;composition:string;gsm:number|string;price:number|string}[]}}>("/ai/use-case-match",{method:"POST",body:JSON.stringify({description:trimmed})});const suggestions=match.data.matches.slice(0,3).map(item=>`${item.name} — ${item.composition}, ${item.gsm} GSM, ₹${item.price}/m`).join("\n");add({role:"assistant",content:suggestions?`Embedding-matched materials for your use case:\n${suggestions}`:"I could not find a matching material lot."});return;}if(/^(search|find|show me|looking for)\b/i.test(trimmed)){const search=await api<{data:{filters:Record<string,unknown>}}>("/ai/natural-search",{method:"POST",body:JSON.stringify({query:trimmed})});sessionStorage.setItem("threadmark-ai-filters",JSON.stringify(search.data.filters));add({role:"assistant",content:"I translated your request into marketplace filters and opened matching material lots."});window.location.assign("/marketplace");return;}const result=await api<{data:{message:string;action?:{type:string;productId:string;productName:string;quantity:number}|null}}>("/ai/chat",{method:"POST",body:JSON.stringify({messages:[...messages,{role:"user",content:trimmed}],productId:sessionStorage.getItem("threadmark-ai-product-id")??undefined})});let reply=result.data.message;if(result.data.action?.type==="add_to_cart"){try{await api("/cart/items",{method:"POST",body:JSON.stringify({productId:result.data.action.productId,quantity:result.data.action.quantity})});reply+=`\n\nAdded ${result.data.action.quantity}m of ${result.data.action.productName} to your cart.`}catch(cause){reply+=`\n\nI found ${result.data.action.productName}, but could not add it: ${cause instanceof Error?cause.message:"cart update failed"}`}}add({role:"assistant",content:reply});if("speechSynthesis" in window)window.speechSynthesis.speak(new SpeechSynthesisUtterance(reply));}catch{add({role:"assistant",content:"I am temporarily unavailable. Browse and filters remain available while I reconnect."});}finally{setThinking(false);}}function listen(){const Recognition=window.SpeechRecognition??window.webkitSpeechRecognition;if(!Recognition){setVoiceError("Voice input is not supported in this browser.");return;}const recognition=new Recognition();recognition.lang="en-IN";recognition.continuous=false;recognition.interimResults=false;recognition.onresult=(event:{results:{[index:number]:{[index:number]:{transcript:string}}}})=>setText(event.results[0][0].transcript);recognition.onerror=()=>setVoiceError("I could not hear that. Try again or type your request.");recognition.start();}return <><button onClick={()=>setOpen(!isOpen)} className="swatch-tag fixed bottom-5 right-5 z-40 border border-indigo-dye bg-indigo-dye px-4 py-3 text-sm font-semibold text-cotton shadow-lg">Ask ThreadMark AI</button>{isOpen&&<section aria-label="ThreadMark AI assistant" className="swatch-tag fixed bottom-20 right-5 z-40 flex h-[min(600px,calc(100vh-110px))] w-[min(400px,calc(100vw-40px))] flex-col border border-loom bg-cotton shadow-2xl"><header className="border-b border-loom px-5 py-4"><p className="font-data text-[10px] uppercase tracking-[.14em] text-ochre">Marketplace assistant</p><h2 className="font-display text-2xl">Ask about fabric.</h2></header><div ref={feed} className="flex-1 space-y-3 overflow-y-auto p-4">{messages.map((message,index)=><div key={index} className={`max-w-[90%] rounded-sm p-3 text-sm leading-6 ${message.role==="assistant"?"swatch-tag border border-loom bg-[#f7f1e7]":"ml-auto bg-indigo-dye text-cotton"}`}>{message.content}</div>)}{isThinking&&<div className="swatch-tag max-w-[90%] border border-loom bg-[#f7f1e7] p-3 text-sm">Thinking…</div>}</div><form onSubmit={(e)=>{e.preventDefault();void send();}} className="border-t border-loom p-3"><label className="sr-only" htmlFor="ai-message">Ask ThreadMark AI</label><div className="flex gap-2"><input id="ai-message" value={text} onChange={e=>setText(e.target.value)} placeholder="e.g. lightweight cotton under ₹500/m" className="min-w-0 flex-1 border border-loom bg-[#f7f1e7] px-3 py-2 text-sm"/><button type="button" onClick={listen} className="border border-indigo-dye px-3 text-indigo-dye" aria-label="Speak your request">Mic</button><button className="bg-indigo-dye px-3 text-sm font-semibold text-cotton">Send</button></div>{voiceError&&<p role="alert" className="mt-2 text-xs text-danger">{voiceError}</p>}</form></section>}</>}
