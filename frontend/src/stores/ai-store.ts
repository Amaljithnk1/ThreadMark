"use client"; import {create} from "zustand"; export type ChatMessage={role:"user"|"assistant";content:string}; type AIState={messages:ChatMessage[];isThinking:boolean;isOpen:boolean;setOpen:(open:boolean)=>void;add:(message:ChatMessage)=>void;setThinking:(value:boolean)=>void}; export const useAIStore=create<AIState>((set)=>({messages:[{role:"assistant",content:"Tell me the end use, fabric, quantity, or specification you need."}],isThinking:false,isOpen:false,setOpen:(isOpen)=>set({isOpen}),add:(message)=>set((state)=>({messages:[...state.messages,message]})),setThinking:(isThinking)=>set({isThinking})}));
